{
  "pluginAlias": "Cosa",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "email": {
        "title": "Email",
        "type": "string",
        "required": true,
        "description": "The email to log in to Cosa."
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "description": "The password to log in to Cosa."
      }
    }
  },
  "uiSchema": {
    "email": {
      "ui:widget": "email"
    },
    "password": {
      "ui:widget": "password"
    }
  }
}
